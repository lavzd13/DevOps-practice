services:
  app:
    build:
      context: app
      dockerfile: Dockerfile
    depends_on:
      db:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.devops-practice.rule=Host(`devops-api.l0mke.com`)"
      - "traefik.http.routers.devops-practice.entrypoints=websecure"
      - "traefik.http.routers.devops-practice.tls.certresolver=letsencrypt"
      - "traefik.http.services.devops-practice.loadbalancer.server.port=8000"
    env_file:
      - .env
    networks:
      - app_network
      - web

  db:
    image: postgres:16
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s
    env_file:
      - .env
    networks:
      - app_network
    volumes:
      - ./entrypoint.sql:/docker-entrypoint-initdb.d/entrypoint.sql
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

networks:
  app_network:
  web:
    external: true